# Required — shared secret used to authenticate requests from the app.
# In production, set via: fly secrets set SCRAPER_SECRET=<value>
SCRAPER_SECRET=

# Restrict CORS to your deployed app's origin (default: *)
ALLOWED_ORIGIN=

# Server port — fly.io sets this automatically; default 8080 locally
# PORT=8080
